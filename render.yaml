services:
  - type: web
    name: ai-findsubject
    runtime: node
    buildCommand: npm install && pip install python-docx pdfplumber
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
